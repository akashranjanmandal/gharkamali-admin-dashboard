# Admin Web — copy to .env.local and fill in your values
NEXT_PUBLIC_API_URL=https://gkm.gobt.in/api
# Google Maps JS key for the geofence / zone map pickers (HTTP-referrer restricted browser key)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-browser-key
